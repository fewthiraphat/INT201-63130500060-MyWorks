const myFunc = (instructor, ta , ...student) => {
        let greeting = "Hello"
        return student;
}

const echo = (text) => {
    return text;
}
const echoText = text => {text}

const sayHi = (greeting , name) => {
    return `${greeting} ${name}`
}

const getClass = () => {
    return 'INT201'   
}